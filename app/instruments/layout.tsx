import type { ReactNode } from 'react';

export const metadata = {
  title: 'Instruments | IBEX Web Dashboard'
}
export default function ClientLayout({ children }: {children: ReactNode}) {
  return children
}
